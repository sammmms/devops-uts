version: "3.8"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Containerfile
    ports:
      - "5001:5000"
    volumes:
      - ./backend:/app
    environment:
      - WATCHFILES_FORCE_POLLING=true

  frontend:
    build:
      context: ./frontend
      dockerfile: Containerfile
    ports:
      - "3001:3000"
    volumes:
      - ./frontend:/app
    environment:
      - VITE_API_BASE_URL=http://localhost:5001/api/v1
    depends_on:
      - backend
